# Configuration du serveur Aether Identity

# Clé secrète pour JWT (utilisée pour signer les tokens)
JWT_SECRET=votre-cle-secrete-ici

# Durée d'expiration des tokens d'accès (en minutes)
ACCESS_TOKEN_EXP=15

# Durée d'expiration des tokens de rafraîchissement (en minutes)
REFRESH_TOKEN_EXP=720

# URL de la base de données PostgreSQL
DATABASE_URL=host=localhost user=postgres password=postgres dbname=aether_identity port=5432 sslmode=disable

# Port sur lequel le serveur écoute
PORT=8080

# Clé système pour les requêtes internes de l'application
# Cette clé est utilisée par l'application web (app/app/) pour authentifier
# les requêtes internes. Elle est considérée comme une clé "système" et
# ne doit être utilisée que par l'application elle-même.
# Générez une clé sécurisée pour la production : sk_ + 15 caractères aléatoires
# Utilisez le script scripts/generate_system_key.sh pour générer une clé sécurisée
SYSTEM_KEY=sk_system_default_key_change_in_production
